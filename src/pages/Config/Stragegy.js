import { Grid } from '@mui/material';

const Stragegy = () => {
    return (
        <Grid container rowSpacing={4.5} columnSpacing={2.75}>
            <Grid item xs={12} sx={{ mb: -2.25 }}>
                <div style={{ paddingBottom: '6px' }}>Stragegy</div>
            </Grid>
        </Grid>
    );
};

export default Stragegy;
